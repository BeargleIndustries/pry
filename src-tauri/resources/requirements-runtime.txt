# Pry managed runtime requirements (installed by src-tauri/src/bootstrap/pip.rs)
# Torch and TL/SAELens are installed separately with specific index URLs.
starlette>=0.40,<1
uvicorn[standard]>=0.32,<1
pydantic>=2.9,<3
safetensors>=0.4,<1
huggingface_hub>=0.26,<1
hf_transfer>=0.1,<1
sse-starlette>=2.1,<3
numpy>=1.26,<3
requests>=2.32,<3
